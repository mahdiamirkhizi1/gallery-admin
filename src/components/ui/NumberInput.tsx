type Props={value:number;onChange:(value:number)=>void;decimals?:number;placeholder?:string;className?:string;min?:number;max?:number};
export function NumberInput({value,onChange,decimals=0,placeholder,className,min,max}:Props){const formatted=value===0?"":new Intl.NumberFormat("en-US",{maximumFractionDigits:decimals}).format(value);return <input className={className} inputMode={decimals?"decimal":"numeric"} value={formatted} placeholder={placeholder} min={min} max={max} onChange={event=>{const raw=event.target.value.replace(/,/g,"");if(raw===""||/^\d*(\.\d*)?$/.test(raw))onChange(Number(raw||0))}}/>}
