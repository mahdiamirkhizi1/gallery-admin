import { ArrowLeft, Box, PackagePlus, UserPlus, Gavel, ClipboardPlus, Percent } from "lucide-react";
import { Card } from "@/components/ui/Card";
import type { ApiProduct } from "../dashboard.types";

export function ProductPanels({ products }: { products: ApiProduct[] }) { const low=products.filter(item=>item.totalStock<=5); return <><Card title="موجودی رو به اتمام"><div className="compact-list">{low.length ? low.map(item=><div key={item.id}><span className="product-placeholder"><Box/></span><p><strong>{item.title}</strong><small>شناسه: {item.id}</small></p><b>{item.totalStock} عدد</b></div>):<p className="muted-message">محصول کم‌موجودی در نتایج فعلی نیست.</p>}</div></Card><Card title="پرفروش‌ترین محصولات"><div className="compact-list">{products.slice(0,4).map((item,index)=><div key={item.id}><span className="product-placeholder"><Box/></span><p><strong>{item.title}</strong><small>موجودی {item.totalStock}</small></p><b>{new Intl.NumberFormat("fa-IR").format(index+1)}</b></div>)}</div></Card></>; }
export function QuickAccess(){const items=[[PackagePlus,"افزودن محصول"],[ClipboardPlus,"ثبت سفارش"],[Percent,"ایجاد تخفیف"],[Box,"مدیریت موجودی"],[UserPlus,"افزودن مشتری"],[Gavel,"ایجاد مزایده"]] as const;return <Card title="دسترسی سریع"><div className="quick-grid">{items.map(([Icon,label])=><button key={label}><Icon/><span>{label}</span><ArrowLeft size={13}/></button>)}</div></Card>}
